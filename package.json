{
  "name": "baseball-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "tsc": "tsc --noEmit",
    "lint": "node scripts/lint.mjs",
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.js"
  }
}
